language: java

jdk:
  - openjdk8
  - oraclejdk11


cache:
  directories:
    - $HOME/.m2/

install:
  - mvn install -DskipTests=true -Dmaven.javadoc.skip=true

after_success:
  - bash <(curl -s https://codecov.io/bash)
