spring:
  profiles:
    active: dev

  # As Spring Cloud Ribbon is in maintenance mode
  cloud:
    loadbalancer:
      ribbon:
        enabled: false

#ribbon:
#  eager-load:
#    enabled: true
#    clients: core, boot

# Jetty / Reactive
coral:
  jetty:
    config:
      enabled: true
    initial-value:
      max-thread: 500
      min-thread: 20
  reactive:
    request:
      enabled: true